import React from 'react';

// SwipeUpGesture component for triggering bottom sheet
interface SwipeUpGestureProps {
  children?: React.ReactNode;
  onSwipeUp?: () => void;
}

export const SwipeUpGesture: React.FC<SwipeUpGestureProps> = () => {
  return null;
};